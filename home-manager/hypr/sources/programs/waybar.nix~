{ config, pkgs, libs, ... }: {
  programs = {
    waybar = {
      enable = true;
      systemd = {
        enable = true;
        target = "hyprland-session.target";
      };
      
      settings = [
        microbar = {
          






        };
      ];
      style = "";

    };
  };
}
